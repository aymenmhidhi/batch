spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/customer_batch_dev
    username: dev_user
    password: dev_password
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    com.example.batch: DEBUG

app:
  batch:
    input-directory: ./data/input/dev
